# Adapters

Adapters wrap external systems behind small typed actions. They should return minimal data and named errors only.
Current providers include `gmail` for mailbox access and `gsheets` for spreadsheet reads and row appends.
