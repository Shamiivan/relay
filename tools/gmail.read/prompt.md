Use `gmail.read` after `gmail.search` when the search results are ambiguous or when you need the full body before answering.

When you refer to a message after reading it, identify it with concrete metadata such as subject, sender, and date.
