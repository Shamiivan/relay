Update an existing local text file inside the current workspace.

Use this for exact surgical edits to a known file.

- the file must already exist
- `oldText` must match exactly once
- the tool writes atomically
- paths must stay inside the workspace root
- binary content is not supported
