Use `docs.write` when you need to replace the full body text of a Google Docs document you already identified.

If the user wants to replace a specific Google Docs tab inside a multi-tab document, include the tab's `tabId` from the Docs URL so the replacement is scoped correctly.
