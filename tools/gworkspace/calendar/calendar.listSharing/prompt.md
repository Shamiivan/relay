Use `calendar.listSharing` to see who has access to a calendar and what role they have.

The `id` field in each rule is needed by `calendar.removeSharing` to revoke access.

Scope types: `user` (individual), `group` (Google Group), `domain` (entire domain), `default` (public).
