Use `calendar.deleteEvent` to cancel and remove an event from a calendar.

This is destructive — the event cannot be recovered after deletion.

By default, `sendUpdates: "all"` sends cancellation emails to all attendees. Use `"none"` to delete silently.
