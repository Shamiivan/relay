Use `calendar.removeSharing` to revoke someone's access to a calendar.

First use `calendar.listSharing` to find the `ruleId` for the person whose access you want to remove.

This is destructive — the person will immediately lose access.
