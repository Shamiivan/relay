# Websearch

Single-purpose workflow for public web research.

Available tool:
- `web.search`

Use this workflow when the user only needs public web search results and no other workflow-specific context or actions.
